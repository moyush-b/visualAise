import { useToast } from "@/hooks/use-toast"

const MediaUploader = () => {
    const { toast } = useToast()

    return (
        <div>MediaUploader</div>
    ) // Use within transformation form (WIP)
}

export default MediaUploader